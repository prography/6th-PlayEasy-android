<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />


    <Switch
        android:id="@+id/alramPermission"
        android:layout_width="310dp"
        android:layout_height="55dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="69dp"
        android:switchMinWidth="100dp"
        android:switchPadding="20dp"
        android:text="알림 권한"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/my_toolbar" />

    <Switch
        android:id="@+id/matchAlram"
        android:layout_width="309dp"
        android:layout_height="55dp"
        android:layout_marginTop="8dp"
        android:switchMinWidth="100dp"
        android:switchPadding="20dp"
        android:text="매치 알림 받기"
        app:layout_constraintEnd_toEndOf="@+id/alramPermission"
        app:layout_constraintStart_toStartOf="@+id/alramPermission"
        app:layout_constraintTop_toBottomOf="@+id/alramPermission" />

    <Switch
        android:id="@+id/scoutAlram"
        android:layout_width="310dp"
        android:layout_height="56dp"
        android:layout_marginTop="8dp"
        android:switchMinWidth="100dp"
        android:switchPadding="20dp"
        android:text="스카우트 알림 받기"
        app:layout_constraintEnd_toEndOf="@+id/matchAlram"
        app:layout_constraintStart_toStartOf="@+id/matchAlram"
        app:layout_constraintTop_toBottomOf="@+id/matchAlram" />


</androidx.constraintlayout.widget.ConstraintLayout>